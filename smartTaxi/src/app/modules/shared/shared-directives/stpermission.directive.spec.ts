import { STPermissionDirective } from './stpermission.directive';

describe('STPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new STPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
